services:
    smtp-project:
        build:
            dockerfile: Dockerfile
        ports:
            - "8000:8000"
        volumes:
            - .:/app
        environment:
            - PYTHONUNBUFFERED=1
        networks:
            - constructionAutomation

networks:
  constructionAutomation:
    external: true